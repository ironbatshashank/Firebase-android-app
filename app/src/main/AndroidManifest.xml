<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shashank.GamuChacha">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Strategy"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".BestLoot" />
        <activity android:name=".BaseLayout" />
        <activity
            android:name=".HelpSupport"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".BestAttack" />
        <activity android:name=".Videos" />
        <activity android:name=".UploadActivity"></activity>
    </application>

</manifest>